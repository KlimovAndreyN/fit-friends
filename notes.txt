+++++++++++++++++++++++++
! перед сдачей прокомпилировать и запустить под Linux!
! везде где используется оставил 'crypto' - import { randomUUID } from 'node:crypto';
+++++++++++++++++++++++++

id2486639
Сдача:
  1. description.md
    1.1. Документация OpenAPI
    1.2. Описание env для backend
    1.3. Запуск backend
    1.4. Запуск frontend
    1.5. Описание сценариев backend
    1.6. Описание сценариев frontend
  2. Сборка и запуск docker-образа в how-to-run.md и описание env?
  3. Архив: zip без сжатия, только исходники, можно удалять маркапы(20мб)?
    имя файла: 2486639-fitfriends.zip

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------
tom@local.ru
swagger@local.ru
123456
--------------
Вопросы:
  хистори? перепроверить все ли корректно, возможно заменить как было изначально, только с типизацией вопрос решить
    history = createBrowserHistory()
    history.push
    useNavigate

  тренировка
    сделать расчет скидки на беке и возвращать в RDO
      а как в детальной карточке отразить скидку?
  
  а как выйти? может, какая кнопка "dispatch(logoutUser())" или автовыход...
    Bad Request; Require logout. Token:
    и на странице с опросом
  
  Создание тренировки
    Фоновые изображения используются предопределённые из макета (вёрстки). При сохранении тренировки присваивается случайное изображение
    подписка на тренера
      Реализованы сценарии раздела «отправки уведомления на почту».
  Реализовать сценарии раздела «Редактирование карточки тренировки тренером»
  Мои заказы
  
  много для сервиса - request: RequestWithRequestIdAndBearerAuthAndUser
  сделать однотипно, т.е. передавать из api / проверять в сервисе роль и т.д. пример: this.httpService.axiosRef.post<BasicQuestionnaireRdo>(this.getUrl(), createDto, headers)
    может передавать централизованно?
  при создании тренировки можно вернуть меньше данных this.httpService.axiosRef.post<BasicDetailTrainingRdo>(this.getUrl(), createDto, headers) и не делать дополнительно преобразование
  
  !сделать времянки, а потом моки
    Friends
    UsersCatalog
    MyOrders
    MyPurchases
  
  Реализован сценарий баланс пользователя - бэкенд
  Реализована покупка тренировки
  Реализованы сценарии раздела отзывы: список отзывов и создание отзывов
    пагинация нужна?
  Реализован сценарий раздела покупка

  видео можно через embed в попап оне например

----

  ../../backend/
  /fronted-index
  from 'libs/shared/....
  @prisma/client
  ' as '
--------------
  на главной у среднего блока <section class="special-offers"
    появляется вертикальная прокрутка так как
    не указан "position: relative; overflow: hidden;"
    как у остальных секций special-for-you/popular-trainings/look-for-company
  различее в страницах
    <html lang="ru" class="js-focus-visible" data-js-focus-visible="">
    <html lang="ru">
    + <canvas> в html
  что за скрипты в маркапах ?
    <script src="js/vendor.min.js"></script>
    <script src="js/main.min.js"></script>
  главная - специальное предложение
    в макете нет ссылки перехода на карточку тренировнки, добавил на заголовок

--------------

  иногда на главной при F5 почемуто происходит разлогинивание, токены из store удаляются
    скорее всего из-за 4 асинхронных запросов с глявной, которые по очереди перевызыаются и сбраcывают ключ
      либо их засинхронизировать или объеденить их данные на api в одном запросе! - добавил
        все равно выкинуло....  какбы vite + reload + F5 и не сбросился статус авторизации????
        на некоторых страницах и главной не было приват роута... добавил
      есть похожие вызовы в тренировке: тренировка и отзывы, если что сделать также
      еще проверить где dispatch вызван больше одного раза
    !можно поискать аналог criticalSection для JavaScript
    !может сделать одно действие fetchIndexPageData, а там синхронно вызвать нужные действия, и таже для других страниц
    пока снова await
    !может сделать получение на API сразу всего?
  /*
  useEffect(() => {
    const fetchData = async () => {
      // попытка решить проблему при обновления токенов, т.к. вызовы были асинхронные, а получение токенов происходит при ошибке выполениня запроса,
      // то были сдучаю удаления токенов при втором запросе, т.к. первый уже получил токены, а второй обновлял старые, а их уже нет...
      await dispatch(fetchForSportsmanTrainings());
      await dispatch(fetchSpecialTrainings());
      await dispatch(fetchPopularTrainings());
      await dispatch(fetchLookForCompanyUserProfiles());
    };

    fetchData();
  }, [dispatch]);
  */

--------------

export const EnumStatus = {
  ERROR: 'ERROR',
  SUCCESS: 'SUCCESS'
} as const;

export type EnumStatus = (typeof EnumStatus)[keyof typeof EnumStatus];

const enumStatus: EnumStatus = EnumStatus.ERROR;
console.log(enumStatus);
--------------
//! отладка
const entries = formData.entries();
for (const entry of entries) {
  const [key, value] = entry;
  // eslint-disable-next-line
  console.log(key, value);
}
//

--------------
//! отладка
// eslint-disable-next-line
console.log('func - value', value);

//! отладка
console.log('func - value', value);
--------------

интерцепторы для HttpService возможны? добавлять requestId, авторизацию...
как перебрать несколько childrens: JSX.Element[]; и если и так и один children: JSX.Element


------------------------------------------

npm install @golevelup/nestjs-rabbitmq
npm install @nestjs-modules/mailer # npm audit fix
npm install @nestjs/axios
npm uninstall @nestjs/common  # "@nestjs/common": "^11.1.0",
npm install @nestjs/common@11.0.20 # для @golevelup/nestjs-rabbitmq
npm install @nestjs/config
npm uninstall @nestjs/core # "@nestjs/core": "^11.1.0",
npm install @nestjs/core@11.0.20 # для @golevelup/nestjs-rabbitmq

npm install @nestjs/jwt
npm install @nestjs/mongoose
npm install @nestjs/passport
npm install @nestjs/platform-express
npm install @nestjs/serve-static
npm install @nestjs/swagger
npm install @prisma/client
npm install axios
npm install bcrypt
npm install class-transformer
npm install class-validator
npm install dayjs
npm install fs-extra
npm install handlebars
npm install joi
npm install mime-types
npm install mongoose
npm install passport
npm install passport-jwt
npm install passport-local
npm install reflect-metadata
npm install rxjs


npm install --save-dev @eslint/js
npm install --save-dev @nestjs/schematics
npm install --save-dev @nestjs/testing
npm install --save-dev @nx/eslint
npm install --save-dev @nx/eslint-plugin
npm install --save-dev @nx/jest
npm install --save-dev @nx/js
npm install --save-dev @nx/nest
npm install --save-dev @nx/node
npm install --save-dev @nx/web
npm install --save-dev @nx/webpack
npm install --save-dev @nx/workspace
npm install --save-dev @swc-node/register
npm install --save-dev @swc/core
npm install --save-dev @swc/helpers
npm install --save-dev @types/bcrypt
npm install --save-dev @types/fs-extra
npm install --save-dev @types/jest
npm install --save-dev @types/mime-types
npm install --save-dev @types/multer
npm install --save-dev @types/node
npm install --save-dev @types/passport
npm install --save-dev @types/passport-jwt
npm install --save-dev @types/passport-local
npm install --save-dev eslint
npm install --save-dev eslint-config-prettier
npm install --save-dev jest
npm install --save-dev jest-environment-node
npm install --save-dev nx
npm install --save-dev prettier
npm install --save-dev prisma
npm install --save-dev ts-jest
npm install --save-dev ts-node
npm install --save-dev tslib
npm install --save-dev typescript
npm install --save-dev typescript-eslint
npm install --save-dev webpack-cli

------------------------------------------



