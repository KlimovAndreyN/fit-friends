+++++++++++++++++++++++++
! перед сдачей прокомпилировать и запустить под Linux!
! везде где используется оставил 'crypto' - import { randomUUID } from 'node:crypto';
+++++++++++++++++++++++++

id2486639
Сдача:
  1. description.md
    1.1. Документация OpenAPI
    1.2. Описание env для backend
    1.3. Запуск backend
    1.4. Запуск frontend
    1.5. Описание сценариев backend
    1.6. Описание сценариев frontend
  2. Сборка и запуск docker-образа в how-to-run.md и описание env?
  3. Архив: zip без сжатия, только исходники, можно удалять маркапы(20мб)?
    имя файла: 2486639-fitfriends.zip

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------
tom@local.ru
swagger@local.ru
swaggercoach@local.ru
123456
--------------
const prevLocation = useAppSelector(getPrevLocation);
dispatch(setPrevLocation(AppRoute.SignIn));

Вопросы:
  Реализовать персональные тренировки
    проверить:
      друзья
      готовность к тренировкам у не инициатора
    что делать если удалили из друзей? или пользователь сменил готовность?
  Реализовать совместные тренировки
    совместная одна для обоих? или можно две?
  Допы
    Реализовать попап с картой - пока сделалана просто картинка
    Реализовать Работу оповещений

  --
  Перепроверить логику не обновления данных, если были на детальной карточке тренировки/пользователя, и сброс фильтров
    для страниц главная, каталог тренировок, мои тренировки, каталог пользователей, мои друзья
      не загружает данные если начали с главной, посомтрели несколько карточек и перешли в каталог - не выполняется setPrevLocation(AppRoute.Index)
      возможно setPrevLocation(AppRoute.Index) можно выполнять в App?
    может для гнавной обновлять когда по лого... а когда назад с каталогов и т.д. не обновлять данные, в backbutton очищать признак активности главной...

  Из-за получения подготовленных childrens в props происходит полная перерисовка страницы
    либо прокинуть обработчики для формирования элементов и серекторы передать пропсами
    или заново передать
    проверить на переделааном, т.к. фильрт меняется и происходит обновление, при нажании слеюущуюя страница, вызвать получение данных
  
  При отрисовке списка бить на части помельче. фильтре, страница и т.д., может страницу вытащить из фильтра и сделать отдельно

  тренировка + редактирование
    скидка
      при просмотре карточки тренером всегда доступна кнопка или при режиме редактирования
      при 0 обработать, скидку некуда применять, а признак можно поставить?
    цена
      от спортсмена и при просмотре от тренера это строка с $, а от тренера при редактировании это число в input
    подписка на тренера
      при созданиии тренировки уведомлять, но не сразу... как то странно по ТЗ...

  просмотр карточки тренера этим же тренером?
    отображать? или 404?
    запрос для отображения его тренировок с ошибкой 403...

  может сделать Layout? а то при загруке главной мигает спинер...
    или для главных страниц сделать спинер с основным оформлением этих страницы (лого, заголовок, спинер)
    Стартовый спиннер проверки авторизации оставить как есть, а при загрузка страниц сделать второй - страничный спиннер
    может и 404 передалть в такой  вид...

  --
  Реализован сценарий баланс пользователя - бэкенд
  Реализована покупка тренировки
  Реализованы сценарии раздела отзывы: список отзывов и создание отзывов
    пагинация нужна?
  Реализован сценарий раздела покупка

  --
  Реализован сценарий редактирование тренировки
  Реализован сценарий «Мои заказы»
  Реализована страница «Мои заказы»
  Реализованы задачи страницы «Карточка тренировки для тренера» (редактирование данных)
  Реализовано дополнительное задание «Реализован сервис для уведомления по email»
    MyOrders
    MyPurchases

  а как выйти на странице с опросом или при ошибке авторизации "Bad Request; Require logout. Token:"
    может автовыход - проверить или какая нибуть кнопка "dispatch(logoutUser())" или ссылка

----
  ../../backend/
  /fronted-index
  from 'libs/shared/....
  @prisma/client
  ' as '
--------------
  на главной у среднего блока <section class="special-offers"
    появляется вертикальная прокрутка так как
    не указан "position: relative; overflow: hidden;"
    как у остальных секций special-for-you/popular-trainings/look-for-company
  различее в страницах
    <html lang="ru" class="js-focus-visible" data-js-focus-visible="">
    <html lang="ru">
    + <canvas> в html
  что за скрипты в маркапах ?
    <script src="js/vendor.min.js"></script>
    <script src="js/main.min.js"></script>
  главная - специальное предложение
    в макете нет ссылки перехода на карточку тренировнки, добавил на заголовок

--------------

  иногда на главной при F5 почемуто происходит разлогинивание, токены из store удаляются
    бывает при нескольких открых вкладах в браузере, при сохранении в VSCode, все страницы сразу обновляются и у некоторых выходит сообщение об авторизации
    скорее всего из-за 4 асинхронных запросов с глявной, которые по очереди перевызыаются и сбраcывают ключ
      либо их засинхронизировать или объеденить их данные на api в одном запросе! - добавил
        все равно выкинуло....  какбы vite + reload + F5 и не сбросился статус авторизации????
        на некоторых страницах и главной не было приват роута... добавил
      есть похожие вызовы в тренировке: тренировка и отзывы, если что сделать также
      еще проверить где dispatch вызван больше одного раза
    !можно поискать аналог criticalSection для JavaScript
    !может сделать одно действие fetchIndexPageData, а там синхронно вызвать нужные действия, и таже для других страниц
    пока снова await
    !может сделать получение на API сразу всего?
  /*
  useEffect(() => {
    const fetchData = async () => {
      // попытка решить проблему при обновления токенов, т.к. вызовы были асинхронные, а получение токенов происходит при ошибке выполениня запроса,
      // то были сдучаю удаления токенов при втором запросе, т.к. первый уже получил токены, а второй обновлял старые, а их уже нет...
      await dispatch(fetchForSportsmanTrainings());
      await dispatch(fetchSpecialTrainings());
      await dispatch(fetchPopularTrainings());
      await dispatch(fetchLookForCompanyUserProfiles());
    };

    fetchData();
  }, [dispatch]);
  */

--------------

export const EnumStatus = {
  ERROR: 'ERROR',
  SUCCESS: 'SUCCESS'
} as const;

export type EnumStatus = (typeof EnumStatus)[keyof typeof EnumStatus];

const enumStatus: EnumStatus = EnumStatus.ERROR;
console.log(enumStatus);
--------------
//! отладка
const entries = formData.entries();
for (const entry of entries) {
  const [key, value] = entry;
  // eslint-disable-next-line
  console.log(key, value);
}
//

--------------
//! отладка
// eslint-disable-next-line
console.log('func - value', value);

//! отладка
console.log('func - value', value);
--------------

интерцепторы для HttpService возможны? добавлять requestId, авторизацию...
как перебрать несколько childrens: JSX.Element[]; и если и так и один children: JSX.Element


------------------------------------------
------------------------------------------

